<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Arka plan rengi (isteğe bağlı, saydam yapabilirsiniz) -->
    <solid android:color="@android:color/transparent" />

    <!-- Kenar çizgisi -->
    <stroke
        android:width="1dp"
        android:color="#000000" /> <!-- Uygulama mavi renginizi kullandım -->

    <!-- Köşe yuvarlaklığı (isteğe bağlı) -->
   />

    <!-- İçerik ile kenarlar arasında boşluk bırakmak için (isteğe bağlı) -->
    <padding
        android:left="4dp"
        android:top="4dp"
        android:right="4dp"
        android:bottom="4dp" />
</shape>